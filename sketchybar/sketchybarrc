# YB sketchybar config — intentionally minimal
# All bar configuration is done by runners/bar.sh at workspace launch time.
# This file is loaded by sketchybar on startup via symlink from ~/.config/sketchybar/sketchybarrc
